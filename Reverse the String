#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    char str[1000];
    int i;
    fgets(str, 1000, stdin);
    for(i= strlen(str)-1; i>=0; i--){
        printf("%c", str[i]);
    }
        
    return 0;
}
