#include <stdio.h>

int main() {
    int n;
    scanf("%d", &n);

    int arr[n], res[n];
    int k = 0;

    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    for (int i = 0; i < n; i++) {
        int found = 0;
        for (int j = 0; j < k; j++) {
            if (arr[i] == res[j]) {
                found = 1;
                break;
            }
        }
        if (!found) {
            res[k++] = arr[i];
        }
    }

    for (int i = 0; i < k; i++) {
        printf("%d", res[i]);
        if (i < k - 1)
            printf(" ");
    }

    return 0;
}
