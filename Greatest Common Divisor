#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int A, B, i;
    scanf("%d %d", &A, &B);
    int min;
    if(A<B){
        min=A;
    }else{
        min=B;
    }
    for(i=min; i>=1; i--){
        if(A%i==0 && B%i==0){
            printf("%d\n", i);
            break;
        }
    }
    return 0;
}
