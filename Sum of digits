#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int i,n,sum=0,digit;
    scanf("%d", &n);
    for(i=n; i>0; i=i/10){
        digit= i%10;
        sum += digit;
    }
    printf("%d\n", sum);    
    return 0;
}
