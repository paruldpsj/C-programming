#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int x, y, sum=0;
    int *p1, *p2;
    
    p1 = &x;
    p2 = &y;
    
    scanf("%d %d", &x, &y);
    
    sum = *p1 + *p2;

    printf("%d", sum);
    return 0;
}
