#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int x, y,z;
    scanf("%d %d", &x, &y);
    int *p1, *p2;
    p1= &x;
    p2= &y;
    z= *p1;
    *p1= *p2;
    *p2= z;
    printf("%d %d", *p1, *p2);
    return 0;
}
